# coding: utf-8
usuarios
a
a = pd.DataFrame(usuarios, index=['a', 'b', 'c', 'd'], columns=['nombre', 'calificaion])
a = pd.DataFrame(usuarios, index=['a', 'b', 'c', 'd'], columns=['nombre', 'calificaion'])
a
a = pd.DataFrame(usuarios, index=['a', 'b', 'c', 'd'], columns=['nombre', 'calificacion'])
a
